#include <stdlib.h>
#include <time.h>
#include "raylib.h"
#include "boids.h"

int main(){
    
    // seed for rand_normalize
    srand(time(NULL));
   
    // define screen size
    float screenWidth = 800;
    float screenHeight = 450;
}